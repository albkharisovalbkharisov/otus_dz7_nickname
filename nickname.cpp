#include <cstdlib>
#include <iostream>
#include <string>
#include <set>

class node
{
	std::string data;
	bool is_end;
	std::set<node> nexts;

	operator 

public:
	void add(const std::string &n)
	{
		;
	}
}

int main(int argc, char ** argv)
{
	for(std::string line; std::getline(std::cin, line); ) {
		b.parse_line(line);
	}

	return 0;
}

